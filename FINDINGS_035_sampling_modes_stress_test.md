# FINDINGS_035 — §31b stress-test: Ganzhi / Mayan / Sefirot / Syntegration

**Date:** 2026-06-15
**Trigger:** kati "keep going" — pressure-test the just-locked §31b (OPERATOR SAMPLING MODES) against the systems named as the stress-test set.
**Discipline:** validation direction only (§0). Do NOT reverse-engineer counts to fit (esp. Mayan/Argüelles per OQ-SYNC-01). For each system: does its structure fit §31b's grammar — **product of substrate-primitive axes (actor × localization × phase) glued by the rulership morphism**, one-or-more engines per system?
**Outcome:** §31b holds. Two systems are clean PRODUCT-engines (Ganzhi, Mayan); two foreground the **morphism/incidence half** §31b already names (Sefirot graph, Beer icosahedron). No system breaks the grammar; the test sharpens it into two components (product + morphism), both already in §31b.

## Results

### Ganzhi (60) — PASS, pure product (it IS Babylonia's localization engine)
60 = 10 stems × 12 branches, restricted to same-polarity pairs (= LCM(10,12)). In §31b terms: **actor (10 = planetary octave / PE stems) × localization (12 = branches/signs)**, polarity-constrained. This is literally the **60-grid** — Babylonia's own localization axis (canon §15d). Strongest validation: an external system's count reproduces Babylonia's localization product exactly.

### Mayan Tzolk'in (260) — PASS as product, with the OQ-SYNC caution
260 = **13 × 20**. 13 = Neptune ring-integer (canon: 65 = 5×13 = Venus×Neptune octave); 20 = dodecahedron-vertices / icosahedron-faces (the 20-count primitive, now substrate-confirmed post Path-D). So 260 = product of two substrate quantities → a §31b product-engine candidate. **Caveat (OQ-SYNC-01):** 260 also = 4×65 (Argüelles/Law-of-Time); the register flags this as reverse-engineering risk. Per §31b's own caveat: count-factoring = **validation, not proof**. Recorded as candidate-correspondence, not a lock.

### Sefirot / Tree of Life — EXTENDS §31b (morphism half foregrounded)
The Tree is **10 nodes + 22 path-edges (+ 3 pillars)** = 32 "paths of wisdom" (10+22). This is a **node-edge GRAPH**, not a pure product — at first glance a sum (10+22), which would seem to fail. But it doesn't: it foregrounds the **two components §31b already names**:
- **10 sefirot = the product/actor axis** (= 10 PE points, canon OQ-SEFIROT-01); **3 pillars = 3 oct axes** (a localization/structural axis).
- **22 path-edges = the MORPHISM/incidence structure** — which node connects to which. §31b's "rulership morphism (localization→actor)" generalizes to the edge/incidence layer. The 22 paths (= 3 mother/elements + 7 double/planets + 12 simple/signs) are the *connections*, not a sampling axis.
So the Tree = product-nodes + morphism-edges. Consistent with §31b once you read the edges as the morphism, not as a coordinate. (Validation-not-proof caveat applies; OQ-SEFIROT-01/02 stay open — correspondence-log, kati/source gated.)

### Beer Syntegration — EXTENDS §31b (polytope incidence)
12 topics @ vertices + 30 members @ edges + 20 teams @ faces = the **icosahedron** (12V/30E/20F exact, OQ-SYNTEQ-01). Same pattern as Sefirot: **12 vertices = product/actor axis** (the 12 Olympians/ico-vertices — now substrate-confirmed post Path-D), **30 edges + 20 faces = the incidence/morphism layer**. A direct geometric correspondence onto a confirmed substrate solid.

## Finding — §31b has two components, both already in the lock
The stress-test clarifies (does NOT change) §31b: a sampling has **(A) product axes** (actor × localization × phase) AND **(B) a morphism/incidence layer** (the rulership map / edges / faces connecting them). Systems differ in which half they foreground:
- **Product-foregrounded:** I Ching, astrology, Ganzhi, Mayan, Babylonia's T(49,60), tarot's Chronos/Tensor engines.
- **Morphism/incidence-foregrounded:** Sefirot (graph: nodes+edges), Beer (polytope: V+E+F), tarot's Kairos engine (the hexad-edge traversal).
Both halves are §31b. A "sum" like 10+22 (Tree) or 12+30+20 (ico) is NOT a grammar violation — it's the node-count + edge-count + face-count of one incidence structure (the morphism layer enumerated by dimension), exactly as a multi-engine system lists engines.

## Disposition
- §31b **survives the stress-test**; scope sharpened to its two named components (product + morphism). No re-lock needed; this is confirming engine-evidence.
- **PASS (product):** Ganzhi 60, Mayan 260 (latter with OQ-SYNC caution).
- **EXTENDS (morphism/incidence):** Sefirot, Beer syntegration — consistent with §31b's morphism component.
- All correspondence-log OQs (SEFIROT-01/02, SYNC-01, SYNTEQ-01) **stay OPEN** — closing them is kati/source-intake gated; this is validation, not proof (§0 Level-A/B). Register annotated with the stress-test result.
- Candidate refinement for a future §31b amendment (NOT auto-locked): name the two components explicitly (product-axes vs morphism-incidence) and the "foregrounding" distinction. Needs kati_direct.

## Files
- Probe: `~/Enki/scripts/probe_oq_operator_sampling_modes.py` (extend with these 4 systems as a follow-up).
- Sources: canon §31b, §15d (Ganzhi), §00a prime-symmetry; register OQ-SEFIROT/SYNC/SYNTEQ; FINDINGS_033.
